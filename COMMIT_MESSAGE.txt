🎯 STEP 34: Implement ASTRIXA Compiler Skeleton

✅ Created clean compiler skeleton that can:
   - Read .ax files  
   - Tokenize source code (Lexer)
   - Parse into AST (Parser)
   - Print AST for debugging

📝 Changes:
   - Created token.rs: Token enum defining language alphabet
   - Updated lexer.rs: Lexer that converts text → tokens
   - Updated ast.rs: AST structures (Expr, Stmt) for language structure
   - Updated parser.rs: Parser that converts tokens → AST
   - Updated main.rs: Wired everything together with simple test

🔧 Files Modified:
   compiler/src/token.rs (NEW)
   compiler/src/lexer.rs (SIMPLIFIED)
   compiler/src/ast.rs (SIMPLIFIED)
   compiler/src/parser.rs (SIMPLIFIED)
   compiler/src/main.rs (SIMPLIFIED)

🎉 This is a real working compiler foundation!
   Just like how Rust, Go, and Zig started.

📚 Implementation follows standard compiler design:
   Source Code → Lexer → Tokens → Parser → AST → (Future: Interpreter/VM)

⚡ How to test:
   cd compiler
   cargo run

Expected output:
   [
       Function {
           name: "greet",
           body: [],
       },
   ]

Next steps: 
   - Add interpreter or bytecode VM
   - Implement expression parsing
   - Add more language features incrementally
