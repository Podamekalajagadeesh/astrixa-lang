contract Counter {
    let count

    fn init() {
        count = 0
    }

    fn increment() {
        count = count + 1
    }

    fn getCount() {
        return count
    }
}

fn main() {
    print(chain.id)
    print(msg.sender)
    print(tx.timestamp)
}
