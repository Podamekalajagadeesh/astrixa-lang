# STEP 40: VISUAL ARCHITECTURE 🎨

**Runtime + Standard Library Integration**

---

## 📊 Complete Pipeline

```
┌──────────────────────────────────────────────────────────┐
│                    ASTRIXA SOURCE                         │
│                                                           │
│                    fn main {                              │
│                      println(42)                          │
│                    }                                      │
└────────────────────────┬─────────────────────────────────┘
                         │
                         ▼
┌──────────────────────────────────────────────────────────┐
│                    LEXER & PARSER                         │
│  Input:  Source code                                      │
│  Output: AST with Expr::Call("println", [42])            │
└────────────────────────┬─────────────────────────────────┘
                         │
                         ▼
┌──────────────────────────────────────────────────────────┐
│                    TYPE CHECKER                           │
│  Validates: Function exists, types match                  │
│  Output: Verified AST                                     │
└────────────────────────┬─────────────────────────────────┘
                         │
                         ▼
┌──────────────────────────────────────────────────────────┐
│                      LOWERING                             │
│  AST → IR transformation                                  │
│                                                           │
│  if is_stdlib_function("println"):                        │
│    emit CallStd("println")                                │
│  else:                                                    │
│    emit Call("println", 1)                                │
│                                                           │
│  Output IR:                                               │
│    LoadConstInt(42)                                       │
│    CallStd("println")      ← NEW!                         │
│    LoadConstInt(0)                                        │
│    Return                                                 │
└────────────────────────┬─────────────────────────────────┘
                         │
                         ▼
┌──────────────────────────────────────────────────────────┐
│                      OPTIMIZER                            │
│  Constant folding, dead code elimination                  │
│  Output: Optimized IR (CallStd preserved)                 │
└────────────────────────┬─────────────────────────────────┘
                         │
                         ▼
┌──────────────────────────────────────────────────────────┐
│                  WASM CODE GENERATOR                      │
│                                                           │
│  Step 1: Scan for CallStd instructions                    │
│    collect_stdlib_imports(module)                         │
│    → {"println"}                                          │
│                                                           │
│  Step 2: Generate imports                                 │
│    (import "env" "println" (func $println (param i32)))   │
│                                                           │
│  Step 3: Generate function body                           │
│    i32.const 42                                           │
│    call $println        ← References import               │
│    i32.const 0                                            │
│    return                                                 │
│                                                           │
│  Output: Complete WASM module with imports                │
└────────────────────────┬─────────────────────────────────┘
                         │
                         ▼
┌──────────────────────────────────────────────────────────┐
│                    WASM MODULE (WAT)                      │
│                                                           │
│  (module                                                  │
│    (import "env" "println" (func $println (param i32)))   │
│                                                           │
│    (func $main (result i32)                               │
│      i32.const 42                                         │
│      call $println                                        │
│      i32.const 0                                          │
│      return                                               │
│    )                                                      │
│    (export "main" (func $main))                           │
│  )                                                        │
└────────────────────────┬─────────────────────────────────┘
                         │
                         ▼
┌──────────────────────────────────────────────────────────┐
│                   NODE.JS RUNTIME                         │
│                                                           │
│  const astrixaStdlib = {                                  │
│    env: {                                                 │
│      println: (value) => {                                │
│        console.log(value);  ← Actual implementation       │
│      }                                                    │
│    }                                                      │
│  };                                                       │
│                                                           │
│  WebAssembly.instantiate(wasm, astrixaStdlib)            │
│    .then(obj => obj.instance.exports.main());            │
└────────────────────────┬─────────────────────────────────┘
                         │
                         ▼
┌──────────────────────────────────────────────────────────┐
│                       OUTPUT                              │
│                         42                                │
└──────────────────────────────────────────────────────────┘
```

---

## 🔄 CallStd Flow

```
┌─────────────────┐
│  ASTRIXA Code   │
│  println(42)    │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│   AST Node      │
│  Call("println",│
│    [Number(42)])│
└────────┬────────┘
         │
         ▼
┌─────────────────┐      ┌──────────────────────┐
│   Lowering      │─────→│ is_stdlib_function?  │
│                 │      │   "println"          │
└────────┬────────┘      └──────────┬───────────┘
         │                          │ YES
         ▼                          ▼
┌─────────────────┐      ┌──────────────────────┐
│   IR            │      │   CallStd            │
│  LoadConstInt   │      │                      │
│  CallStd(...)   │←─────│  (not Call)          │
└────────┬────────┘      └──────────────────────┘
         │
         ▼
┌─────────────────┐
│  WASM Codegen   │
│  Scan CallStd   │
│  Generate import│
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  WASM Output    │
│  (import "env"  │
│   "println" ...)│
│  call $println  │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│   Runtime       │
│  Provides impl  │
│  println: (v)=> │
│   console.log(v)│
└─────────────────┘
```

---

## 🧩 Component Interactions

```
┌──────────────┐
│  Compiler    │
│              │
│  - Declares  │─────────┐
│    stdlib    │         │
│  - Emits     │         │
│    CallStd   │         │
└──────────────┘         │
                         │ WASM Import
                         │ Declaration
                         ▼
              ┌──────────────────┐
              │   WASM Module    │
              │                  │
              │  (import "env"   │
              │   "println" ...) │
              └──────────────────┘
                         │
                         │ Import
                         │ Request
                         ▼
┌──────────────┐         │
│   Runtime    │         │
│              │◄────────┘
│  - Implements│
│    stdlib    │
│  - Provides  │
│    to WASM   │
└──────────────┘
```

---

## 🏗️ Runtime Architecture

```
┌────────────────────────────────────────────────┐
│              Node.js Runtime                    │
│                                                 │
│  ┌──────────────────────────────────────────┐  │
│  │        Stdlib Implementation             │  │
│  │                                          │  │
│  │  print:   (v) => process.stdout.write()  │  │
│  │  println: (v) => console.log(v)          │  │
│  │  ...more stdlib functions...             │  │
│  └──────────────────────────────────────────┘  │
│                     ▲                           │
│                     │ Provides                  │
│  ┌──────────────────┴───────────────────────┐  │
│  │       WebAssembly.instantiate            │  │
│  │                                          │  │
│  │  Loads WASM                              │  │
│  │  Connects imports to stdlib              │  │
│  │  Executes main()                         │  │
│  └──────────────────────────────────────────┘  │
└────────────────────────────────────────────────┘
```

---

## 📦 ABI (Application Binary Interface)

```
┌─────────────────────────────────────────────────┐
│              ASTRIXA STDLIB ABI                  │
├─────────────────────────────────────────────────┤
│                                                  │
│  Function: print                                 │
│    Import:   "env" "print"                       │
│    Params:   (i32)                               │
│    Returns:  void                                │
│    Behavior: Print value without newline         │
│                                                  │
│  Function: println                               │
│    Import:   "env" "println"                     │
│    Params:   (i32)                               │
│    Returns:  void                                │
│    Behavior: Print value with newline            │
│                                                  │
│  [Future: malloc, free, read, write, etc.]       │
│                                                  │
└─────────────────────────────────────────────────┘
```

---

## 🔍 Code Generation Detail

```
IR Instructions:              WASM Output:
                             
LoadConstInt(42)     ─────►  i32.const 42
CallStd("println")   ─────►  call $println
LoadConstInt(0)      ─────►  i32.const 0
Return               ─────►  return

Plus module header:          (import "env" "println" 
                              (func $println (param i32)))
```

---

## 🌐 Multi-Runtime Vision

```
                  ┌──────────────┐
                  │ ASTRIXA WASM │
                  │   (same)     │
                  └───────┬──────┘
                          │
         ┌────────────────┼────────────────┐
         │                │                │
         ▼                ▼                ▼
    ┌─────────┐     ┌─────────┐     ┌─────────┐
    │ Node.js │     │ Browser │     │ Wasmtime│
    │ Runtime │     │ Runtime │     │ Runtime │
    └─────────┘     └─────────┘     └─────────┘
         │                │                │
         ▼                ▼                ▼
    ┌─────────┐     ┌─────────┐     ┌─────────┐
    │ Server  │     │   Web   │     │  Smart  │
    │  App    │     │   App   │     │Contract │
    └─────────┘     └─────────┘     └─────────┘
```

---

## 📊 Data Flow

```
STEP 1: Compile Time
────────────────────
Source → AST → IR (with CallStd) → WASM (with imports)


STEP 2: Runtime
────────────────────
WASM Module
    │
    ├─ Import "println" needed
    │     │
    │     └─ Runtime provides: (v) => console.log(v)
    │
    └─ Execute main()
         │
         └─ call $println(42)
              │
              └─ Runtime executes: console.log(42)
                   │
                   └─ Output: 42
```

---

## 🧠 Design Decisions

### Why Host-Powered?

```
Option A: Compile stdlib into WASM
┌────────────┐
│   WASM     │ ← Large, fixed implementation
│ (with all  │
│  stdlib)   │
└────────────┘

Option B: Host-Powered (ASTRIXA's choice)
┌────────────┐
│   WASM     │ ← Small, flexible
│  (imports) │
└─────┬──────┘
      │
      ▼
┌────────────┐
│  Runtime   │ ← Swappable, updatable
│  (stdlib)  │
└────────────┘

Benefits:
✅ Smaller WASM
✅ Runtime can be updated independently
✅ Different implementations for different hosts
✅ Security boundary at import level
```

---

## 🎯 Key Architectural Choices

### 1. Separation of Concerns
```
Compiler: What to call
Runtime:  How to implement
```

### 2. Thin Runtime
```
Runtime has minimal logic
Most capabilities from host (Node.js)
```

### 3. Clean ABI
```
CallStd = clear boundary
Versioning at ABI level
Easy to extend
```

### 4. Extensibility
```
Add function: 3 places
  1. is_stdlib_function()
  2. generate_import()
  3. runtime implementation
```

---

## 🏆 Achievement

```
╔════════════════════════════════════════════╗
║                                            ║
║         BEFORE          →      AFTER       ║
║                                            ║
║    Compiler only        │   Full Pipeline  ║
║    No I/O               │   Real Output    ║
║    Theoretical          │   EXECUTABLE     ║
║                                            ║
║           🚀 ASTRIXA IS ALIVE 🚀           ║
║                                            ║
╚════════════════════════════════════════════╝
```

---

**Status:** ✅ COMPLETE  
**This is how modern languages work: Rust, Go, Zig all use this pattern.**
