// Comprehensive test for all language core fixes
// Tests: comments, booleans, floats, modulo operator

fn test_all_features() {
    // Test 1: Comments work (single-line)
    let message = "Comments work!"
    
    // Test 2: Boolean literals
    let is_true = true
    let is_false = false
    
    // Test 3: Float literals
    let pi = 3.14159
    let e = 2.71828
    let temp = 98.6
    
    // Test 4: Modulo operator
    let remainder = 10 % 3
    let even_check = 8 % 2
    
    // Test 5: All operators together
    let result = 10 + 5 * 2 - 3 / 1 % 2
    
    // Test 6: Boolean expressions with comparisons
    if is_true == true {
        println("Boolean comparison works")
    }
    
    // Test 7: Float arithmetic
    let float_result = pi + e
    let float_mult = pi * 2.0
    
    // Test 8: Nested comments and complex expressions
    let complex = (10 % 3) + (5 * 2) // inline comment
    
    return 0
}

// Final comment
fn main() {
    // Call the test function
    test_all_features()
}
